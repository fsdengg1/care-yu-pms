# CareYu PMS Frontend — public build variables only.
#
# Leave NEXT_PUBLIC_API_URL empty for local Next.js rewrites to BACKEND_URL.
# Set it to the public API origin when the frontend is hosted separately.
NEXT_PUBLIC_API_URL=

# Local Next.js rewrite target (next dev / next start).
BACKEND_URL=http://localhost:4100
